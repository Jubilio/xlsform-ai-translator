# TypeScript type checking

Run the complete client and server TypeScript validation with:

```bash
npm run typecheck
```

The command checks both `tsconfig.json` and `tsconfig.server.json` without emitting build files.
